# Map transition overlay improvements

Delayed the "Waiting for response..." overlay during map transitions so it only appears after 3 seconds, keeping the game world visible during fast transitions. Added a guard against double-transitions from rapid teleport requests.
